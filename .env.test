# ----------------------------------
# PostgreSQL Configuration (Testing)
# ----------------------------------
POSTGRES_HOST=localhost
POSTGRES_PORT=5433  # Puerto diferente al de producción
POSTGRES_USER=admin_test
POSTGRES_PASSWORD=TestPassword456!
POSTGRES_DB=transportadora_test
POSTGRES_URL=postgresql://${POSTGRES_USER}:${POSTGRES_PASSWORD}@${POSTGRES_HOST}:${POSTGRES_PORT}/${POSTGRES_DB}?schema=public

# ----------------------------------
# Application Environment
# ----------------------------------
NODE_ENV=test